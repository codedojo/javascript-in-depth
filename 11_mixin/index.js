'use strict';

/**
 * Переносит все свойства (кроме свойств прототипа) из объектов `sources` в объект `target`
 * @param {Object} target - целевой объект, в который будут скопированы свойства
 * @param {...Object} sources - объекты-источники из которых будт копироваться свойства
 */
function mixin() {
    
}



module.exports = mixin;