{
    "name": "javascript-in-depth",
    "version": "1.0.0",
    "scripts": {
        "test": "jest --verbose",
        "test:watch": "jest --verbose --watch"
    },
    "dependencies": {
        "jest": "^20.0.4"
    },
    "jest": {
        "testEnvironment": "node"
    }
}