1. Создайте функцию-конструктор `Character`, которая создает объект следующей формы

```ts
{
    name: string,
    hp: number = 100
}
```

2. Создайте объект, используя функцию `Character` и поместите его в переменную `character`.